name: guardrails-stop-94-25
on:
  push:
    branches: [chore/guardrails-stop-94-25]
  pull_request:
    branches: [main, master, chore/ci-cd-break-before-prod, chore/guardrails-stop-94-25]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  guardrails:
    name: guardrails 3 camadas
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - run: npm ci --no-audit --no-fund
      - run: node scripts/guardrails-check.mjs
  injection:
    name: injection 30+ PT-BR/EN
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - run: npm ci --no-audit --no-fund
      - run: node scripts/guardrails-check.mjs
  hallucination:
    name: hallucination golden >94%
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - run: npm ci --no-audit --no-fund
      - run: node scripts/guardrails-check.mjs
  cost:
    name: cost SLM <25%
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - run: npm ci --no-audit --no-fund
      - run: node scripts/guardrails-check.mjs
  break-gate:
    name: break before prod
    runs-on: ubuntu-latest
    needs: [guardrails, injection, hallucination, cost]
    steps:
      - run: echo "GATES VERDES: merge liberado, deploy liberado com poll ate SUCCESS"
